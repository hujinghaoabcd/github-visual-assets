# Architecture Diagram Template Guidance

Use a left-to-right flow when the process is sequential; use layered vertical structure for hierarchical models; use swim lanes when system components have different responsibilities.

Keep node styles consistent, limit the main palette to neutrals plus one family color, label tensor/data dimensions only when they add explanatory value, and provide an SVG master whenever practical.
