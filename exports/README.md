# Exports

Publish-ready files are grouped by format: `svg/`, `png/`, `webp/`, and `gif/`.

Exports should be optimized, stripped of unnecessary editor metadata, visually checked in light and dark GitHub themes where relevant, and stable enough for cross-repository reuse.
