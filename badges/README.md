# Badges

Use Shields.io for dynamic status badges where possible. Keep custom static SVG badges in `badges/custom/` and reusable Shields.io snippets in `badges/shields/`.

Avoid excessive badge walls: use badges to communicate build status, package version, license, DOI, documentation, supported Python versions, or other meaningful metadata.
