# Asset Browser Site

Future Astro-based browsing/search layer. The site should consume `manifest/` and canonical repository paths rather than copying tens of thousands of assets into `site/public/`.

Planned capabilities: search, category/tag/source filters, variant grouping, light/dark preview, copy raw URL/Markdown/HTML, download, favorites and provenance display.
