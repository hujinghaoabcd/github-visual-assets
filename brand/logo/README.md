# Logo System

Keep definitive brand marks here. Store editable masters under `source/svg/` and publish optimized SVG/PNG variants under `exports/`.

Recommended variants: primary, horizontal, icon-only, monochrome, light-background and dark-background.
