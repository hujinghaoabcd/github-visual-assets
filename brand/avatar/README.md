# Avatar Assets

Target master size: 800 × 800 or larger. Keep safe content inside the central 80% because GitHub displays avatars as circles in many contexts. Store source artwork separately from final exports.
