# Illustrations

Curate reusable illustrations by topic: `coding/`, `research/`, `gis/`, `maps/`, `earth/`, `ai/`, `data/`, `server/`, `weather/`, and `engineering/`.

Prefer vector sources and keep style consistency within each project. Every third-party illustration requires provenance metadata.
