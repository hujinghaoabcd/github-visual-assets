# Projects

One folder per first-party repository. Copy `_template/` when starting a new visual kit. `_unassigned/` is a temporary holding area for legacy project-owned assets that have not yet been mapped to a repository.
